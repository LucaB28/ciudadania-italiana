# Los archivos .bat tienen que quedar con saltos de linea de Windows:
# con saltos de Linux, cmd.exe los interpreta mal y falla a mitad de camino.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
